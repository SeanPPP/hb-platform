## 需求

* 保留“商品图片”和“序号”两列，但这两列不参与排序

* 其它业务列启用前端排序，沿用页面现有的前端排序实现

## 变更点

* 前端页面：`ReactUmi/my-app/src/pages/ContainerDetail/index.tsx`

* 在 react-data-grid 的 `columns` 定义中：

  * 明确为“序号”“商品图片”列设置 `sortable: false`

  * 为其它业务列（文本/数字/金额）统一设置 `sortable: true`（已有的保留，不足的补齐）

* 不改动现有 `useMemo` 的本地排序逻辑与编辑逻辑

## 验证

* 点击“序号”“商品图片”列头不会触发排序

* 点击其它列头可触发排序；列表依据现有 `useMemo` 排序规则更新

* 页面无类型/运行错误

## 影响

* 仅前端显示层改动，后端与接口不变；兼容当前数据结构

