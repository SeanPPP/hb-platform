修改 `SalesDashboardReactService.cs` 的 `GetSupplierSalesRankAsync` 方法：

1. 在当前查询中添加过滤条件 `s.IsDomestic != true`（第409-410行附近）

2. 在同比查询中添加过滤条件 `s.IsDomestic != true`（第452-458行附近）

这样实现：

* `GetSupplierSalesRankAsync` → 只查询本地供应商

* `GetChinaSupplierSalesRankAsync` → 只查询国内供应商（已正确实现）

