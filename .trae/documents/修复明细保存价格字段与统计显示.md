## 原因分析
- “上次进货价”未更新：保存载荷未采用检测到的 `currentPurchasePrice`。
- “零售价列不需要编辑”：应使用检测结果或自动定价计算值保存，无需手工输入。
- 统计为 0：前端仅读取 PascalCase，未兼容 camelCase。

## 变更方案
- 前端（详情页保存）
  - 位置：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx`
  - 保存载荷：
    - `lastPurchasePrice` → 优先 `rowMeta.supplierDetect.currentPurchasePrice`，否则 `it.lastPurchasePrice`。
    - `retailPrice` → 优先 `rowMeta.newAutoRetailPrice`（自动定价计算值），否则 `rowMeta.supplierDetect.currentRetailPrice`，否则 `it.retailPrice`。
    - 移除对 `retailOverride` 的依赖与写入。
  - 零售价列：改为只读显示（不渲染 `InputNumber`），显示值来源同保存优先级。
  - 保存统计解析：兼容 `data.data.inserted/updated/failed` 与 `data.inserted/updated/failed`（camelCase），并保留现有 PascalCase 兼容。
- 后端：无需修改，`BatchUpsertDetailsAsync` 已支持相关字段更新。

## 验证
- 检测后保存：`LastPurchasePrice`/`RetailPrice` 更新成功，刷新详情页显示一致。
- 保存提示：插入/更新/失败计数正确显示。