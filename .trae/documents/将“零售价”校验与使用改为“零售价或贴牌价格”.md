## 要求确认
- 以“行内贴牌价格”为准：不再读取 `商品信息.零售价格`，零售价相关逻辑全部使用行的 `贴牌价格` 字段。

## 调整点
- 资格校验：`贴牌价格 > 0` 且 `进口价格 > 0`。
- 创建 Product：`retailPrice = 行.贴牌价格`，`isAutoPricing=false`，`localSupplierCode='200'`。
- 仓库：`OEMPrice = 行.贴牌价格`，其余不变。
- 分店零售价：`StoreRetailPriceValue = 行.贴牌价格`，`PurchasePrice = 行.进口价格`。
- 多码分店价：`MultiCodeRetailPrice = 行.贴牌价格`，`PurchasePrice = 行.进口价格`。
- 错误提示：改为“贴牌价格≤0”并列出货号示例。

## 变更位置
- `ReactUmi/my-app/src/pages/ContainerDetail/index.tsx` 的 `handleCreateNewProducts` 中：
  - 替换所有 `零售价格` 使用为 `row.贴牌价格`
  - 更新资格判断、提示文案、payload 映射与 upsert 值

## 验证
- 针对 HB239-029：贴牌价为 $4.50，满足新建要求，不会提示“贴牌价格≤0”。
- 其它逻辑不变（英文名、进口价、上架状态与分店/多码联动）。