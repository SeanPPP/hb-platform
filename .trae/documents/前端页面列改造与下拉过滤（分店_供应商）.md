## 目标
- 在“分店商品价格管理”页面新增并可编辑 `IsSpecialProduct` 列（复选框）。
- 将“分店名称”“供应商名称”的过滤控件改为下拉框，选项从对应控制器获取：
  - 分店：`GET /api/Stores/active`
  - 本地供应商：`GET /api/react/v1/local-suppliers/active`

## 具体改动（ReactUmi 前端）
1) 页面文件：`ReactUmi/my-app/src/pages/PosAdmin/StoreRetailPrices/index.tsx`
- 列定义：
  - 在 `columns` 中新增 `{ data: 'isSpecialProduct', title: '是否特殊商品', type: 'checkbox' }`
- 编辑收集：
  - 在 `afterChange` 中，当 `prop === 'isSpecialProduct'` 时，额外收集到 `specialMap`（productCode 与目标布尔值），其余字段仍走 `editedMap`。
- 保存逻辑：
  - 在 `saveChanges` 中，先处理 `editedMap` 调用 `batchUpsert`，再按 `specialMap` 中的 true/false 分组分别调用 `batchUpdateSpecial({ productCodes, isSpecial })`。
- 下拉过滤：
  - 增加 `storeOptions`、`supplierOptions` 状态与加载函数；页面初始化加载这两个列表。
  - 将现有的两个 `Input` 替换为 `Select`（可清空，多选或单选）；
  - 在 `buildGridRequest` 中，`storeName`/`supplierName` 使用 `FilterType: 'text', Type: 'equals', Filter: 选中值`（后端无需改动）。

2) 服务与类型
- 文件：`ReactUmi/my-app/src/types/storeRetailPrice.ts`
  - 已新增 `isSpecialProduct?: boolean`；继续新增 `BatchUpdateSpecialRequestDto`（已完成）。
- 文件：`ReactUmi/my-app/src/services/storeRetailPriceService.ts`
  - 已新增 `batchUpdateSpecial`；再新增 `getActiveStores()` 与 `getActiveLocalSuppliers()` 方法以供页面取下拉选项。

## 后端（已具备支持）
- 列投影、排序、过滤已加入 `IsSpecialProduct`；
- 批量更新接口已实现：`PUT /api/react/v1/store-retail-prices/batch-special`。

## 验证
- 构建前端，打开页面：
  - 看到新增列“是否特殊商品”并可勾选；
  - 下拉可以选择分店与供应商进行过滤；
  - 保存后能分别更新价格与特殊商品标记；
  - 排序、分页、关键词仍正常。

确认后我将按照以上步骤修改前端页面与服务，并进行基本功能验证。