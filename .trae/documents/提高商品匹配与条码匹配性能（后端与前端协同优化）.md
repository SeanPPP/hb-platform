**后端分片合并**

* 位置：`BlazorApp.Api/Services/React/LocalSupplierInvoicesReactService.cs`

* 新增通用方法：`private async Task<List<T>> QueryInChunksAsync<T, TKey>(List<TKey> keys, int chunkSize, Func<List<TKey>, Task<List<T>>> fetch)`，按 `chunkSize=500` 切片 `keys`，逐片 `fetch` 并合并去重。

* 接入点：

  * 条码匹配：`DetectBarcodeAsync` 将 `validBarcodes` 的 `Product` 与 `StoreMultiCodeProduct` 两段查询改为 `QueryInChunksAsync`。

  * 供应商+货号匹配：`DetectSupplierItemAsync` 将 `validItemNumbers` 的 `Product` 查询改为 `QueryInChunksAsync`。

  * 分店价格：对 `allCodes/productCodes` 触发 `StoreRetailPrice` 查询时也用 `QueryInChunksAsync`，避免超长 `IN (...)`。

* 并发：内部顺序合并，避免对数据库造成并发放大；如需并发可限制并发度为 3。

**前端防抖**

* 位置：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx` 与 `.../LocalSupplierInvoiceCreate/index.tsx`

* 新增 `useDebouncedEffect` 或引入 `lodash.debounce`，对行级输入（条码、货号、进价）在 300ms 停止输入后触发检测。

* 具体：

  * 行内条码输入框在 `onChange` 更新 state；`useEffect` 监听变更并在防抖后将本行加入待检测队列。

  * 队列批量：每 300–500ms 拉取当前队列，去重并调用批量接口（已有 `bulkDetect*`），回填结果。

**前端批量策略**

* 位置：同上页面与 `src/services/localSupplierInvoiceService.ts`

* 已有批量与并发控制实现（每批 50、并发 3），保留策略并抽象通用工具：

  * 新增 `processBatches<T>(batches: T[][], concurrency: number, worker: (batch: T[]) => Promise<void>)` 于 `src/utils/batch.ts`，两页面皆复用。

  * 统一批大小 50、并发 3，可在环境配置可调。

* 请求去重：在批次组装前对条码/货号列表 `trim + distinct`，减少冗余请求。

**前端虚拟化**

* 位置：`LocalSupplierInvoiceDetail` 明细表与 `LocalSupplierInvoices` 列表页。

* 集成 `react-window` 虚拟化：

  * 明细表：将 `antd Table` 的 `components.body` 替换为虚拟列表渲染器，设置 `scroll={{ y: 600 }}`。

  * 列表页：如数据量较大（>1000），同样启用虚拟化渲染行。

* 关键点：固定行高、禁用会导致重排的样式，分页或无限加载与虚拟化兼容。

**验证与度量**

* 后端：日志记录每段集合大小与 SQL 耗时；对 1k/5k/10k 的条码、货号集做压测，确认 P95 响应时间下降。

* 前端：观察输入体验、请求次数与渲染耗时；在 DevTools Performance 下对虚拟化页进行对比。

**改动范围**

* 新增：`QueryInChunksAsync`；`src/utils/batch.ts` 与 `src/hooks/useDebouncedEffect.ts`。

* 修改：接入分片的匹配方法、页面输入监听

