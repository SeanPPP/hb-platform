## 修复计划

### 1. 修复英文语言包缺失的翻译
在 `ReactUmi/my-app/src/locales/en-US.ts` 中添加缺失的翻译键：
- `menu.salesDashboard.salesDashboardMain`: 'Overview'
- `menu.posAdmin.productManagement`: 'Product Management'
- `menu.posAdmin.cashRegisterUsers`: 'Cash Register Users'
- `menu.warehouse.storeOrder`: 'Store Order'
- `menu.warehouse.storeOrderList`: 'Store Order List'
- `menu.warehouse.storeOrderDetail`: 'Store Order Details'

### 2. 修复中文语言包缺失的翻译
在 `ReactUmi/my-app/src/locales/zh-CN.ts` 中添加：
- `menu.salesDashboard.salesDashboardMain`: '总览'

### 3. 修复重复菜单键问题
修改 `.umirc.ts` 路由配置：
- 修复 `/sales-dashboard` 重复：父级路径改为 `/sales-dashboard`，子路由保持 `/sales-dashboard` 但调整结构避免重复
- 修复 `/posm/sales-orders` 重复：父级路径改为 `/posm/sales-orders`，子路由保持 `/posm/sales-orders` 但调整结构避免重复