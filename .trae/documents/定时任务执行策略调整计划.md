我将按以下步骤修改代码以满足您的需求：

1.  **修改配置** (`BlazorApp.Api/appsettings.json`):
    -   将 `HourlyTaskIntervalMinutes` 从 `60` 修改为 `20`，以满足“每隔20分钟执行一次”的要求。

2.  **修改服务逻辑** (`BlazorApp.Api/Services/Background/ScheduledTaskService.cs`):
    -   在 `ExecuteHourlyTask` 方法的开头添加时间范围判断逻辑。
    -   **开始时间**：每天 08:00。
    -   **结束时间**：
        -   周四：21:00 (9pm)。
        -   其他日子：18:00 (6pm)。
    -   **判断逻辑**：
        -   如果当前时间早于 08:00，跳过执行。
        -   如果当前时间晚于结束时间（例如 18:00 后），跳过执行。
        -   特别处理结束时间的整点（如 18:00），允许执行，但 18:20 及之后不再执行。
    -   现有的 `ExecuteAsync` 初始化逻辑已经能够保证“整点开始”（以及00, 20, 40的对齐），无需修改，只需确保它依赖的配置值已更新为 20。

这样修改后，服务会全天候运行 Timer（每20分钟触发一次），但在非工作时间段内会自动跳过实际的任务执行。