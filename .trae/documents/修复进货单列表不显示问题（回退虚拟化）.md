## 目标
- 将“进货单列表”页面恢复为普通 AntD Table（不使用虚拟化）。

## 变更内容
- 修改 `src/components/VirtualTable.tsx`：移除虚拟化注入（删除 `components.body`），保留原先的 `scroll.y` 高度与分页配置支持。
- 修改 `src/pages/PosAdmin/LocalSupplierInvoices/index.tsx`：始终使用 `antd` 原生 `Table` 组件渲染列表（不再根据 `total` 切换到 `VirtualTable`）。

## 不变部分
- 保留已增强的响应容错解析与 `skipBizReject` 开关，确保后端有数据时前端能展示。
- 保持现有筛选、分页、排序、删除、查看明细的交互行为。

## 验证
- 启动页面，确认列表正常显示；测试筛选、分页、排序与跳转明细。
- 如仍异常，提供 Network 响应样例以比对字段名（容错已覆盖常见键名）。