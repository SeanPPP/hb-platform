## 调整策略（按您的要求）
- 图片来源优先使用 `rowMeta[detailGUID].supplierDetect`，其次才回退到 `record.productImage`。

## 修改方案
1) 辅助函数：`resolveProductImageFromDetect(record, detect)`
- 顺序：
  - 先从 `detect` 读取候选字段：`ProductImage/productImage/ImageUrl/imageUrl/ProductImageUrl/img/image`（按此顺序）
  - 若都为空，再使用 `record.productImage`
- URL 规则：接受 `http(s)://`、以 `/` 开头、或相对路径（直接使用）；为空返回空字符串。

2) 更新列渲染
- 在“商品图片”列的 `render(_: string, r)` 中，获取 `const detect = rowMeta[r.detailGUID]?.supplierDetect`，调用辅助函数并渲染 `<Image>`。
- 保持灰色占位作为兜底。

3) 可选：检测后回填
- 在 `onDetectAll` 完成后，若 `items[i].productImage` 为空且 `detect` 有图片，写入 `items[i].productImage = 解析后的URL`，便于后续保存与分发。
- 仅在空值时写入，不覆盖已有。

## 验证
- 检测结果含图片的行显示图片；已有商品图片为空时也能显示检测图片；无图片仍显示占位。

请确认后我立即按此方案实施。