## 实施步骤

### 1. 创建本地实体模型
- 在 `BlazorApp.Shared\Models\HBweb\` 创建 `CashRegisterUser.cs`
- 映射HQ实体字段到本地实体

### 2. 注册数据库表
- 在 `HqSqlSugarContext.cs` 添加 `DIC_收银用户信息表` 的 SimpleClient
- 在 `SqlSugarContext.cs` 添加 `CashRegisterUser` 的 SimpleClient 和表初始化

### 3. 实现同步服务
- 在 `DataSyncReactService.cs` 实现 `SyncCashRegisterUsersFromHqAsync()` 方法
- 使用完整同步模式（分页读取、批量插入、事务管理）

### 4. 创建API端点
- 创建或修改控制器添加 `POST /api/hqdata/sync/cash-register-users` 端点

### 5. 配置AutoMapper
- 创建映射配置：`DIC_收银用户信息表` → `CashRegisterUser`

### 6. 实现前端UI
- 在 `index.tsx` 添加收银用户同步按钮和状态管理
- 调用API端点并显示同步结果