为了实现动态添加新权限的功能，我将执行以下步骤：

1.  **定义数据传输对象 (DTO)**:
    *   在 `BlazorApp.Shared/DTOs/RoleDtos.cs` 中添加 `CreateSysPermissionDto` 类，包含 `Code` (权限代码), `Name` (显示名称), `Category` (分类), `Description` (描述) 等字段。

2.  **扩展服务接口**:
    *   在 `BlazorApp.Api/Interfaces/IRoleService.cs` 中添加 `CreatePermissionAsync` 方法定义。

3.  **实现服务逻辑**:
    *   在 `BlazorApp.Api/Services/RoleService.cs` 中实现 `CreatePermissionAsync` 方法。
    *   该方法将检查权限代码是否已存在，如果不存在则创建新的 `SysPermission` 记录存入数据库。

4.  **暴露 API 接口**:
    *   在 `BlazorApp.Api/Controllers/RolesController.cs` 中添加 `POST api/roles/permissions` 端点，允许管理员通过 API 创建新权限。

这样，您就可以通过调用 API 动态地向系统中添加新的权限定义，并在后续分配给角色使用。