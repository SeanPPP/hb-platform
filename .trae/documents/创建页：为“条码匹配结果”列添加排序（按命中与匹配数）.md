## 修改点
- 为“条码匹配结果”列添加排序（优先按是否命中 matched，然后按匹配数 matchCount）。
- 在表格上方统计条中显示“供应商+货号 命中: 57”等动态计数（当前已显示 supplierFoundCount，将标签文案改为更明确）。

## 实施文件
- ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceCreate/index.tsx

## 具体实现
- 列定义添加 sorter：
  - `(a, b) => ((a.barcodeDetect?.matched ? 1 : 0) - (b.barcodeDetect?.matched ? 1 : 0)) || ((a.barcodeDetect?.matchCount || 0) - (b.barcodeDetect?.matchCount || 0))`
- 统计标签文案调整：将“供应商命中”改为“供应商+货号 命中”，保持计数逻辑不变。

## 验证
- 点击“条码匹配结果”列头可排序。
- 统计条显示“供应商+货号 命中: N”，与当前检测结果一致。

## 影响范围
- 仅前端创建页，后端无改动。