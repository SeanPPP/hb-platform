## 实施计划：用户管理添加重置密码功能

### 1. 后端修改
- **IUserService.cs** 和 **UserService.cs**:
  - `UpdateUserPasswordByGuidAsync` 方法已经存在，支持接收 `UpdateUserPasswordDto` (包含 `NewPassword`)。
  - 现有的 `ResetUserPasswordAsync` 方法是生成随机密码，不符合"输入2次新密码"的需求。
  - **方案**: 直接复用 `UpdateUserPasswordByGuidAsync` 方法。
- **UsersController.cs**:
  - `[HttpPut("guid/{guid}/password")]` 端点已经存在，映射到 `UpdateUserPasswordByGuidAsync`。
  - **无需新增 API**，直接使用现有端点。

### 2. 前端修改
- **UserManagement/index.tsx**:
  - 在用户列表的操作列添加"重置密码"按钮。
  - 点击按钮弹出一个 Modal。
- **新建组件 UserManagement/components/ResetPasswordModal.tsx**:
  - 表单包含：新密码、确认新密码。
  - 校验规则：必填，两次密码必须一致，长度限制。
  - 提交时调用 `updateUserPassword` API。

### 3. 前端服务
- **services/userService.ts** (假设存在，需要确认):
  - 确保有 `updateUserPassword` 方法，调用 `PUT /api/users/guid/{guid}/password`。

### 4. 实施步骤
1.  检查 `services/userService.ts`，确保有密码更新方法。
2.  创建 `ResetPasswordModal.tsx` 组件。
3.  在 `index.tsx` 中引入并使用该组件。
4.  在表格操作列添加触发按钮。