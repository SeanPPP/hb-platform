我将通过以下步骤为您优化移动端页面适配，并实现顶部导航栏功能：

### 1. 修改样式文件 `src\layouts\StoreLayout.less`

* **添加移动端样式类**：定义 `.mobileHeader` 和 `.mobileNavBar` 及其子元素的样式，包括布局、颜色（深蓝背景）、图标大小等。

* **媒体查询适配**：使用 `@media (max-width: 768px)` 确保在移动端：

  * 隐藏原有的 PC 端顶部栏 (`.topBar`, `.header`, `.navBar`)。

  * 显示新的移动端顶部栏 (`.mobileHeader`)。

  * 调整页面容器间距。

### 2. 修改布局组件 `src\layouts\StoreLayout.tsx`

* **引入必要组件**：导入 Ant Design 的 `Drawer`, `Menu` 组件以及 `MenuOutlined`, `HomeOutlined` 等图标。

* **添加状态管理**：新增 `mobileCategoryVisible` 状态来控制商品分类抽屉的显示。

* **优化交互逻辑**：修改 `handleCategoryClick` 函数，在点击分类后自动关闭抽屉。

* **实现移动端结构**：

  * **Logo 与搜索栏**：在移动端顶部显示 Logo 和搜索框。

  * **四按钮导航栏**：实现 Products（商品）、Menu（菜单）、Account（账户）、Cart（购物车）四个功能按钮。

  * **商品分类抽屉**：点击 Products 按钮时从左侧滑出分类菜单，复用现有的分类数据。

### 3. 验证与微调

* 确保 `StoreHome` 页面的商品网格在移动端正常显示（依赖现有的响应式 Grid）。

* 验证导航按钮的点击跳转和交互效果。

