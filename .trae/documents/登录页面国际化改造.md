我将对登录页面进行国际化改造，使其支持中英文切换，并适配已有的国际化配置。

**分析：**

1. **现有资源**：`locales/zh-CN.ts` 和 `locales/en-US.ts` 中已经包含了大量的国际化键值对，包括 `'login.title'`, `'login.username'`, `'login.password'`, `'login.submit'`, `'login.register'` 等。
2. **待修改文件**：`src/pages/Login/index.tsx` 目前硬编码了中文文本。
3. **技术方案**：使用 `umi` 或 `react-intl` 提供的 `useIntl` 或 `FormattedMessage` 组件来替换硬编码文本。考虑到这是一个 Umi 项目，通常使用 `@umijs/max` 导出的 `useIntl`。

**实施计划：**

1. **修改** **`src/pages/Login/index.tsx`**：

   * 引入 `useIntl` 和 `SelectLang` (用于切换语言)。

   * 将所有硬编码的中文文本替换为 `intl.formatMessage({ id: '...' })`。

   * 涉及的文本包括：

     * 标题/副标题（需要新增 key）："HB Platform", "多店铺订单管理系统", "欢迎回来", "请登录您的账号以继续"。

     * 特性介绍（需要新增 key）："高效管理", "数据分析", "安全可靠"。

     * 表单标签/Placeholder：用户名, 密码, 记住我, 忘记密码, 登录, 注册, 邮箱地址, 确认密码, 两次密码输入不一致。

     * 提示信息：登录成功, 登录失败, 注册功能开发中...

     * 版权信息：© 2026 HB Platform...

   * 在页面右上角添加 `<SelectLang />` 组件以支持用户切换语言。

2. **更新语言文件 (`locales/zh-CN.ts`** **&** **`locales/en-US.ts`)**：

   * 补充缺失的 key，例如首页的宣传语、特性描述等。

**补充 Key 列表 (拟定)：**

* `login.subtitle`: '多店铺订单管理系统' / 'Multi-store Order Management System'

* `login.feature.efficient`: '高效管理' / 'Efficient Management'

* `login.feature.analysis`: '数据分析' / 'Data Analysis'

* `login.feature.secure`: '安全可靠' / 'Secure & Reliable'

* `login.welcome`: '欢迎回来' / 'Welcome Back'

* `login.welcomeDesc`: '请登录您的账号以继续' / 'Please login to continue'

* `login.copyright`: '© 2026 HB Platform. All rights reserved.'

* `login.registerWait`: '注册功能开发中...' / 'Registration feature coming soon...'

* `validation.passwordMismatch`: '两次密码输入不一致' / 'Passwords do not match' (已有)

**执行步骤：**

1. 更新 `zh-CN.ts` 和 `en-US.ts` 添加缺失的翻译。
2. 修改 `Login/index.tsx` 实现国际化并添加语言切换组件。

