## 目标
- 保持“保存更改”不变。
- 在“检测”完成后，页面表格的“上次进货价”列优先显示分店零售价表返回的 `CurrentPurchasePrice`（行级 `supplierDetect.currentPurchasePrice`），无检测数据时回退显示本行的 `lastPurchasePrice`。

## 前端改动
- 文件：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx`
- 列“上次进货价”渲染与排序/筛选：
  - 渲染：`const v = rowMeta[r.detailGUID]?.supplierDetect?.currentPurchasePrice ?? r.lastPurchasePrice;` 显示 `v?.toFixed(2)`。
  - 排序与筛选：对应使用上述 `v` 值，而非仅 `record.lastPurchasePrice`。

## 交互保持
- 检测流程与结果存储（`rowMeta[detailGUID].supplierDetect`）保持原样，无需后端改动。
- 其它列与保存逻辑不变。

## 验证
- 执行“检测”后，“上次进货价”列应实时显示分店当前价；未检测或检测失败时，仍显示原 `lastPurchasePrice`。

确认后我将按上述方案更新前端渲染逻辑并完成验证。