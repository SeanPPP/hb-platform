## 修复方案

在 [SalesDashboard/index.tsx](file:///d:\Development\cline\blazor\ReactUmi\my-app\src\pages\SalesDashboard\index.tsx) 的 `loadDashboardData` 函数中进行以下修改：

### 具体改动

1. **在 `setLoading(true)` 之后立即清除所有旧数据**（约第 58 行后添加）：
   - `setDashboardSummary(null);`
   - `setHourlySales([]);`
   - `setStoreSalesRank([]);`
   - `setSupplierSalesRank([]);`
   - `setChinaSupplierSalesRank([]);`
   - `setSupplierStoreSales([]);`
   - `setStoreSupplierSales([]);`

2. **简化条件判断**（约第 71-89 行）：
   - 将所有 `if (res.success && res.data)` 改为 `if (res.success)`
   - 确保即使 API 返回空数组或 null，也会更新 state

### 效果
- 日期更改后立即清除旧数据，配合 loading 状态显示
- API 返回 `{success: true, data: []}` 时正确显示空状态
- 避免显示过时数据