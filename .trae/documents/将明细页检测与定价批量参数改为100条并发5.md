## 目标
- 条码检测与自动定价的前端批处理参数调整为：每批 100 条、并发 5。

## 变更点
- 文件 `src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx`：
  - onDetectAll：`chunk(indices, 50)` → `chunk(indices, 100)`；`processBatches(..., 3, ...)` → `..., 5, ...`。
  - onEvaluate：批数组构建 `i += 50` → `i += 100`；并发控制 `inFlight < 3` → `inFlight < 5`。
  - batchSetAutoPricing：批数组构建 `i += 50` → `i += 100`；并发控制 `inFlight < 3` → `inFlight < 5`。

## 验证
- 触发“检测全部”与“自动定价”操作，进度提示按批次更新，整体耗时下降且服务器可承受；编译与类型诊断通过。