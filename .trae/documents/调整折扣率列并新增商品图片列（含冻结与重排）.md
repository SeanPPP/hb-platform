## 现状分析

* 折扣率为比例小数，校验范围 0–1，列头“折扣率”：`ReactUmi/my-app/src/pages/PosAdmin/StoreRetailPrices/index.tsx:65–76`

* 列顺序为：分店、供应商、商品名称、货号、条码...：`index.tsx:47–113`

* 当前返回数据不含商品图片字段：`ReactUmi/my-app/src/types/storeRetailPrice.ts:30–46`

* 商品服务提供 `productImage` 字段与详情接口：`ReactUmi/my-app/src/services/product.ts:25–43, 68–72`

## 前端改动

1. 折扣率改为百分制

* 列头改为`折扣率%off`

* 校验范围调整为 `10–90`（允许空值），`numericFormat` 改为适配百分制（如 `0` 或 `0.00`）

* 加载转换：`loadData` 中将后端的 `0–1` 显示为 `10–90`

* 保存转换：`saveChanges` 将用户填写的百分数除以 `100` 写入 `DiscountRate`

1. 新增商品图片列

* 在列首新增 `商品图片`（只读，宽度约 60–80），使用 renderer 输出 `<img src=...>`，占位符处理无图情况

* 数据来源（两种方案）：

  * 优选：后端 `getGrid` 响应增加 `productImage` 字段（性能最佳）

1. 列重排与冻结

* 将 `货号` 调整到 `商品名称` 之前

* 设置左侧冻结 2 列（图片、货号）：`fixedColumnsLeft={2}`

* 更新 `nestedHeaders` 第 1 行的 `colspan`，`基本信息` 增加到 6 列

## 兼容与验证

* 边界值：9/10/90/91 验证、空值通过

* 批量保存后确认后端存储为 `0–1`，刷新显示为百分制

* 图片加载性能与分页（建议页大小≤100，或采用后端扩展）

## 交付范围

* 本次仅修改 `index.tsx` 前端逻辑；若选择后端扩展图片字段，后续追加后端 API 的 join/映射改动说明并配合实现

