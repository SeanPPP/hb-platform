移除标签显示的阈值限制，让所有数据点都显示金额：

1. **合计模式**（无分店过滤）：

   * 移除 `value > 100` 的限制

   * 显示所有数据点的金额标签

   * 保留 `autoHide: false` 确保所有标签都显示

2. **多分店模式**（有分店过滤）：

   * 移除 `value > 0` 的限制，改为显示所有标签

   * 保持 `autoHide: true` 让图表自动处理密集标签的显示

