## 目标
- 在进货单详情页，将工具按钮条从页面顶部移动到“明细（X 条）”卡片标题之后、表格之前显示，视觉上与表格形成一体。

## 改动点
- 文件：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx`
- 将现有的按钮条 `<Space>` 从信息卡片下方的位置，改到明细卡片内部，具体结构：
  - `<Card title={...}>\n  <Space style={{ marginBottom: 8 }}>[检测/计算/批量开关/批量执行/删除]</Space>\n  <Table .../>\n</Card>`
- 保持按钮逻辑与事件处理不变，仅调整渲染位置；按钮顺序与样式保持一致，`marginBottom` 设为 `8` 与卡片内间距协调。
- `Table` 的 `scroll`、`rowSelection` 等不变；固定列和操作列位置不变。

## 验证
- 标题“明细（X 条）”下方显示工具栏；点击各按钮功能正常；表格滚动与布局不受影响。