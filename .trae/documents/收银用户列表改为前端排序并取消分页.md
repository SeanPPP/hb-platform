将收银用户条码管理改为前端排序并取消分页

## 当前实现分析

**后端：**

* `/api/react/v1/cash-register-users/grid` 接口支持分页（`startRow`, `endRow`, `pageSize`）

* 支持服务端排序（`sortModel`）

**前端：**

* 使用 Ant Design Table 的 `pagination` 属性

* 发送分页和排序参数到后端

* 使用独立的 `Pagination` 组件

## 修改方案

### 前端修改

**文件：** `d:\Development\cline\blazor\ReactUmi\my-app\src\pages\PosAdmin\CashRegisterUsers\index.tsx`

**修改内容：**

1. **修改 load 函数**：

   * 移除分页参数，请求所有数据

   * 设置一个很大的 `pageSize`（如 10000）或使用后端支持的方式

   * 移除 `sortModel` 参数（改为前端排序）

2. **修改列定义**：

   * 保留自定义 `sorter` 函数（用于前端排序）

   * 移除 `sortOrder` 绑定（改为使用 `onChange` 处理）

3. **添加 Table 的 onChange 处理**：

   * 监听排序变化

   * 对 `data` 进行前端排序

   * 更新 `data` state

4. **移除分页相关代码**：

   * 移除 Table 的 `pagination` 属性（或设置为 `false`）

   * 移除底部 `Pagination` 组件

   * 移除 `page` 和 `pageSize` state

   * 移除 `sortBy` 和 `sortOrder` state（如果完全前端排序）

5. **修改 useEffect 依赖**：

   * 移除 `page`, `pageSize`, `sortBy`, `sortOrder` 依赖

   * 只在 `keyword`, `storeCode`, `status` 变化时重新加载数据

### 后端修改（可选）

保持现有接口不变，前端通过设置大 `pageSize` 获取所有数据。如果数据量很大，可以考虑：

* 添加一个新接口 `/api/react/v1/cash-register-users/all` 返回所有数据（不分页）

* 或在后端限制最大返回条数（如 10000）

## 实现细节

**前端排序逻辑：**

```typescript
const handleTableChange = (
  pagination: any,
  filters: any,
  sorter: any
) => {
  if (sorter.field && sorter.order) {
    const sortedData = [...data].sort((a: any, b: any) => {
      // 使用现有的列定义中的 sorter 逻辑
    });
    setData(sortedData);
  }
};
```

**请求所有数据：**

```typescript
const res = await getGrid({
  startRow: 0,
  endRow: 9999,
  pageSize: 10000,
  globalSearch: keyword || undefined,
  filterModel: Object.keys(filterModel).length ? filterModel : undefined,
});
```

