## 修改点
- 在列表页头部操作区新增“新增进货单”按钮，点击后打开创建页。
- 位置：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoices/index.tsx`
- 行为：使用现有 `window.g_tabModel.addTab` 在 KeepAlive Tabs 中打开 `/pos-admin/local-supplier-invoices/create`，标题为“新建进货单”。

## 具体实现
- 在表单按钮区（查询/重置）右侧追加一个主按钮“新增进货单”。
- onClick：
  - 调用 `addTab({ key: 'pos-admin-local-supplier-invoices-create', title: '新建进货单', path: '/pos-admin/local-supplier-invoices/create', icon: 'PlusOutlined', component: 'LocalSupplierInvoiceCreate', keepAlive: true })`
- 不影响现有查询与分页逻辑。

## 验证
- 列表页显示新增按钮；点击后打开创建页；可粘贴明细、执行两种检测并保存。

## 影响范围
- 仅前端列表页 UI 增加入口，不改动后端。