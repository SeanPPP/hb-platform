## 接口与对接
- 后端已提供删除接口：
  - 路由：`DELETE /api/react/v1/local-supplier-invoices/{invoiceGuid}`
  - 控制器实现：`BlazorApp.Api/Controllers/React/ReactLocalSupplierInvoicesController.cs:101`
  - 服务签名：`BlazorApp.Api/Interfaces/React/ILocalSupplierInvoicesReactService.cs:10-15`（新增 `DeleteAsync`）
  - 软删除逻辑：置主表与明细 `IsDeleted=true`，并更新 `UpdatedAt/UpdatedBy`，事务提交
    - 服务实现：`BlazorApp.Api/Services/React/LocalSupplierInvoicesReactService.cs:267-344` 与新增删除方法位置 `BlazorApp.Api/Services/React/LocalSupplierInvoicesReactService.cs:...`（新增 `DeleteAsync`）
- 前端调用封装：
  - `deleteInvoice(invoiceGuid)`：`ReactUmi/my-app/src/services/localSupplierInvoiceService.ts:33-52`
- 列表页操作集成：
  - 二次确认弹窗 + 调用删除 + 成功后刷新：`ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoices/index.tsx:79-97`

## 权限与返回
- 角色限制：`Admin,WarehouseManager`
- 返回结构：`{ success: boolean, data: boolean, message?: string }`

## 联调与验证
- 运行后端与前端，打开列表页：
  - 点击“删除”→弹出确认→确认后执行删除；成功提示并刷新，记录消失
  - 失败时显示错误信息
- 验证软删除：数据库中对应主表与明细 `IsDeleted` 字段置为 `true`

## 后续可选优化
- 批量删除支持（多选行）
- 删除前校验单据状态（如已入库不可删）
- 操作日志记录与审计