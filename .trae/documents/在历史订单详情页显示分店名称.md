我将修改“历史订单详情页”，使其显示订单所属的分店名称。

### 1. 修改 StoreHistoryOrders/Detail.tsx

*   **获取分店名称**：
    *   当前详情页仅显示 `order.storeCode`。
    *   我需要复用 `getUserStores` API（类似于列表页的做法），获取当前用户的所有分店信息。
    *   或者，如果 `getOrderDetail` 接口返回的数据中已经包含了 `StoreName`（需要确认接口定义），则直接显示。

*   **检查接口定义**：
    *   查看 `src/services/storeOrder.ts` 中的 `StoreOrderCartDto` 定义，目前没有 `storeName` 字段。
    *   查看后端 `GetOrderDetailAsync` 实现，它返回了 `StoreAddress`，但没有显式返回 `StoreName` 到 DTO 中。虽然 DTO 中没有 `StoreName` 属性，但后端查询时确实关联了 `Store` 表。

*   **方案选择**：
    *   **方案 A (前端查找)**：在详情页组件中调用 `getUserStores`，并在前端根据 `order.storeCode` 查找对应的 `storeName`。这是最快且不修改后端 DTO 的方法。
    *   **方案 B (后端修改)**：修改后端 DTO 和查询逻辑，将 `StoreName` 加入返回结果。这更规范，但涉及后端改动。

    鉴于之前的交互中我修改了前端列表页来显示分店名称，为了保持一致性且快速生效，我将采用 **方案 A (前端查找)**。

### 2. 执行计划
修改 `d:\Development\cline\blazor\ReactUmi\my-app\src\pages\StoreHistoryOrders\Detail.tsx`：

1.  引入 `getUserStores` API 和 `useModel`。
2.  在组件加载时（或 `order` 加载后），调用 `getUserStores` 获取分店列表。
3.  在渲染分店信息时，查找并显示 `storeName`。
    *   将 `<Descriptions.Item label="Store Code">` 修改为显示 `Store Name (Store Code)` 或仅显示 `Store Name`。

