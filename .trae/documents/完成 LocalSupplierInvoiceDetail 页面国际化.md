## 国际化完成计划

### 1. 修复编译错误
- 从 `@umijs/max` 导入 `useIntl`

### 2. 补充国际化 key（zh-CN.ts 和 en-US.ts）
添加以下缺失的翻译：
- 表单字段标签（分店、供应商、随货单号、订单日期、入库日期、入库状态、总金额、备注）
- 筛选按钮（筛选、重置）
- 列标题和筛选相关文本（条码、自动定价、定价浮率、新自动零售价、操作等）
- 按钮文本（返回列表、批量修改操作、应用到所选等）
- 匹配结果相关文本（已存在、未找到、已匹配、未匹配、匹配数>2、查看等）
- 特殊商品相关文本（是、否）
- 匹配商品详情表格列标题（图片、商品类型、供应商、定价等）

### 3. 替换页面中的硬编码文本
- 将所有硬编码的中文文本替换为 `intl.formatMessage()` 调用
- 确保所有列标题、按钮文本、提示信息都使用国际化

### 4. 处理内联样式警告（可选）
- 将所有 `style={{ ... }}` 内联样式移到单独的 CSS 文件中，使用 className 代替