我将根据您的要求，将“历史订单详情页”的商品列表调整为**三列布局**的表格（Table）形式，并适配移动端展示。

### 1. 组件替换
*   将当前的 `List` 组件替换为 Ant Design 的 `Table` 组件。
*   **适配移动端**：在移动端，表格通常会有横向滚动条，或者我们会尽量压缩列宽。考虑到您要求的“三列布局”，在手机竖屏下可能会比较拥挤，但我将通过 CSS 调整确保其可读性。

### 2. 表格列定义 (Columns)
*   **第一列：Product (商品名称/货号)**
    *   **内容**：上面显示商品名称（支持换行），下面显示货号（Item No，灰色小字）。
    *   **对齐**：左对齐。
    *   **排序**：支持按商品名称排序。
    *   **宽度**：Flex 比例较大，或者自适应。

*   **第二列：Price (价格信息)**
    *   **内容**：显示 `Import: $xx.xx / RRP: $xx.xx`。
    *   **格式**：保留两位小数，用 "/" 分隔。
    *   **对齐**：右对齐。
    *   **排序**：支持按价格排序。
    *   **宽度**：固定宽度或自适应。

*   **第三列：Qty (数量信息)**
    *   **内容**：显示 `Order: xx / Sent: xx`。
    *   **格式**：整数，用 "/" 分隔，缺货显示 "0"。
    *   **对齐**：右对齐。
    *   **排序**：支持按订货数量排序。
    *   **宽度**：固定宽度。

### 3. 样式调整
*   **行高**：设置 `rowClassName` 或样式确保行高约为 40px（内容多时会自动撑开）。
*   **表头**：加粗显示。
*   **移动端适配**：
    *   设置 `scroll={{ x: 'max-content' }}` 以允许横向滚动（如果内容过宽）。
    *   或者尽量精简列标题，例如 "Product", "Price(Imp/RRP)", "Qty(Ord/Snt)"。

### 4. 执行计划
修改 `d:\Development\cline\blazor\ReactUmi\my-app\src\pages\StoreHistoryOrders\Detail.tsx`：

1.  引入 `Table` 组件。
2.  定义 `columns` 数组。
3.  替换 `<List>` 为 `<Table>`。
4.  添加必要的 CSS 样式以满足“行高”、“加粗表头”等要求。

**注意**：`StoreOrderCartItemDto` 似乎没有 RRP (建议零售价) 字段。根据之前的代码，有 `oemPrice` (Import) 和 `price`。通常 `price` 是售价。我将假设：
*   Import Price = `oemPrice` (或 `importPrice` 如果存在)
*   RRP = `price` (或者如果不显示 RRP，只显示 Import)。
*   **修正**：之前的代码用了 `oemPrice` 作为 Import Price。如果 DTO 中没有 RRP，我将只显示 Import Price，或者如果 `price` 是 RRP 则显示它。我会检查 DTO 定义。如果无法确定 RRP，我将暂时只显示 Import Price，或者把 `price` 当作 RRP。
*   根据之前的代码片段，`item.price` 曾被用来显示价格。让我们假设 `item.price` 是 RRP，`item.oemPrice` 是进货价。

**更新**：用户要求显示“进货价/建议零售价”。
*   显示格式：`Imp: $10.00 / RRP: $15.00` (如果空间不够，可能需要换行或简化)。
*   为了移动端三列布局，我建议格式为：
    ```
    Imp: $10.00
    RRP: $15.00
    ```
    (上下排列比左右 "/" 分隔更节省横向空间，但用户明确要求 "/" 分隔)。
    好吧，我将严格遵循用户要求的 "/" 分隔。如果移动端太宽，会自动出现滚动条。

