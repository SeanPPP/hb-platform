## 问题分析
- 前端在 `src/app.ts:398` 处统一错误处理，记录了 `500`：`console.error('❌ 请求错误:', error)`。
- 分类页加载调用：
  - 分类树：`src/pages/WarehouseCategories/index.tsx:54-57` → `getCategoryTree()` → `GET /api/react/v1/warehouse-categories/tree`
  - 分类商品：`src/pages/WarehouseCategories/index.tsx:59-66,103-105` → `getProductsByCategory(categoryGuid, filter)` → `GET /api/react/v1/warehouse-categories/{categoryGuid}/products`
- 前后端路由一致：后端控制器 `BlazorApp.Api/Controllers/React/ReactWarehouseCategoriesController.cs:9` 路由 `[Route("api/react/v1/warehouse-categories")]`，上述两个 GET 方法在 `22-35` 与 `149-161`，出现异常会返回 `500`。
- Axios/Umi 拦截器已自动注入 `Authorization`：`src/app.ts:367-388` 与 `src/utils/request.ts`；代理配置正确：`.umirc.ts:155-163`。

## 可能原因（优先级由高到低）
- 后端服务异常：`GetTreeAsync` 或 `GetProductsByCategoryAsync` 内部抛出异常，控制器统一封装为 `500` 返回；对应服务实现在 `BlazorApp.Api/Services/React/WarehouseCategoryReactService.cs:23-42,153-173`。
- 数据库/数据问题：
  - 分类数据存在异常（极小概率重复 `CategoryGUID` 导致 `ToDictionary` 抛错）：`WarehouseCategoryReactService.cs:30`。
  - 关联查询或导航属性引起的异常（`Includes(p => p.Product)`）：`WarehouseCategoryReactService.cs:155`。
- 授权状态边缘问题：虽然类级 `[Authorize]` 一般返回 `401`，但服务内部依赖用户信息时异常也会表现为 `500`。

## 验证步骤
- 在浏览器网络面板检查请求与响应：确认 `GET /api/react/v1/warehouse-categories/tree` 和 `GET /api/react/v1/warehouse-categories/{guid}/products` 的状态码与响应体。
- 查看后端控制台日志：控制器在捕获异常时有日志 `GetTree failed` / `GetProductsByCategory failed`（`ReactWarehouseCategoriesController.cs:32,159`）；`SqlSugarContext`开启了 SQL 打印（`SqlSugarContext.cs:43-47`）。
- 使用 Swagger 在 `http://localhost:5001/swagger` 手动调用两个接口，带上 `Authorization: Bearer <token>`，观察是否必现 `500`，记录错误响应内容。

## 修复方案
- 前端增强容错与提示：
  - 为分类树 `loadTree()` 增加 try/catch 与 `message.error`（与商品列表一致），并避免 UI 崩溃（`src/pages/WarehouseCategories/index.tsx`）。
- 后端稳健性改造（最小改动）：
  - `GetTreeAsync`：将 `ToDictionary` 改为安全构建（跳过重复键或记录错误），并对空/脏数据做防御性处理。
  - `GetProductsByCategoryAsync`：对导航属性为空进行空值保护，确认投影到 `WarehouseProductListDto` 的字段均可空。
  - 控制器 `GetTree` / `GetProductsByCategory`：在 `catch` 中增加详细日志（包含异常信息与请求参数），仍保持对外返回通用消息。
- 诊断辅助：
  - 在日志中添加分类树返回数量与耗时统计，商品分页的总数/页参数，便于快速定位数据层问题。

## 实施步骤
1) 前端：为 `loadTree()` 增加错误处理与用户提示；在网络日志中输出实际命中的 URL 与 Token 携带状态（现拦截器已有日志）。
2) 后端：微调 `WarehouseCategoryReactService` 两个方法的防御性代码（不改变接口契约），并在控制器的 `catch` 中增加上下文日志。
3) 本地验证：
- 通过 Swagger 与前端页面分别验证分类树与商品列表加载是否正常；
- 观察后端控制台是否再出现 `GetTree failed` / `GetProductsByCategory failed`。

确认后我将按上述步骤进行前后端改动与验证。