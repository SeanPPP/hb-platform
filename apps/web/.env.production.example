# 复制为已被 Git 忽略的 .env.production.local 后填写。该值会进入浏览器 bundle，是公开低权限写入 token，不是 secret；不得复用其他 secret。
VITE_CENTER_LOG_KEY=
VITE_CENTER_LOG_PROJECT=hbweb_rv
VITE_CENTER_LOG_ENVIRONMENT=Production
VITE_CENTER_LOG_SERVICE_NAME=hbweb_rv-web
