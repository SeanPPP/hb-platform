分析
- 溢出源于文本列过长、列数过多与固定布局不足；当前容器缺少横向滚动与固定表头配置，文本未做多行省略与断行，数值列未固定宽度。

实施计划（单文件修改：ReactUmi/my-app/src/pages/WarehouseCategories/index.tsx）
1) 表格容器与滚动
- 在 Table 外层包裹 div.warehouse-table-wrapper，容器样式：min-width:100%，overflow-x:auto，确保内容溢出时出现横向滚动条。
- Table 增加：tableLayout="fixed"、scroll={{ x: 1400 }}、sticky。
  • 位置：index.tsx:263-287 Table 定义处。

2) 列定义增强
- 为数字/价格列设置固定宽度：
  • domesticPrice、importPrice、oemPrice：width: 110
  • minOrderQuantity：width: 90
  • status：width: 100
  • createdAt、updatedAt：width: 180
- 文本列统一包裹 <div className="cell-text">，配合 CSS 实现 word-break 与多行省略：
  • itemNumber、barcode、productName、categoryName、supplierName、supplierCode 列添加 render: (t)=> <div className="cell-text">{t}</div>
  • 位置：index.tsx:70-76、78-82 列数组定义。

3) 样式注入（不新建样式文件）
- 在页面 JSX 末尾添加内联 <style>，定义：
  • .warehouse-table-wrapper { min-width:100%; overflow-x:auto; }
  • .warehouse-table-wrapper .cell-text { display:-webkit-box; -webkit-line-clamp:2; -webkit-box-orient:vertical; overflow:hidden; word-break:break-word; }
  • .warehouse-toolbar（给工具栏 Space 添加该 class）：在窄屏下纵向堆叠。
  • @media (max-width: 768px) { .warehouse-toolbar { flex-direction: column; gap: 8px; } }

4) 移动端适配与响应式隐藏
- 使用 AntD 列的 responsive 属性在小屏隐藏非必要列：
  • 隐藏：supplierName、supplierCode、oemPrice、createdAt、updatedAt（responsive: ['md']）
  • 保留：barcode、productName、categoryName、domesticPrice、importPrice、minOrderQuantity、status
- 保持横向滚动，满足“左右滑动查看”。

5) 测试要点
- 桌面：宽度正常与缩窄窗口下，验证横向滚动与固定表头有效；文本多行省略与断行生效；价格列宽固定。
- 移动：≤768px，工具栏垂直堆叠；隐藏列不显示；可左右滑动查看；选择、排序、过滤与批量操作可用。

请确认上述计划，我将开始实施并在完成后进行不同分辨率的显示与操作性验证。