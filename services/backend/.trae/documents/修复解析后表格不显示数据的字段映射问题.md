## 问题分析
- 解析成功但表格为空白，根因是字段名不匹配：
- 表格列使用小写字段（如 `itemNumber/productName/barcode/quantity/purchasePrice/retailPrice/amount`），而解析函数 `parsePastedDetailed` 和旧 `parsePasted` 推入的是大写字段（如 `ItemNumber/ProductName/Barcode/Quantity/PurchasePrice/RetailPrice/Amount`）。
- 结果：行已添加，但 `Table` 的 `dataIndex` 找不到对应属性，导致单元格不显示。

## 修改方案
1. 统一解析输出为小写字段
- 在 `parsePastedDetailed(raw, selectedCols)` 中，将推入对象的键统一改为：
  - `itemNumber`、`productName`、`barcode`、`quantity`、`purchasePrice`、`retailPrice`、`amount`
- 保留 `detailGUID`，其余与 UI 无关的上游字段（如 `InvoiceGUID/StoreCode/SupplierCode`）可以保留或去除，不影响显示。
- 当某列未选择时，对应字段设为 `undefined`；仅当 `quantity>0 && purchasePrice>0` 时计算并设置 `amount`。

2. 同步旧解析函数
- 将 `parsePasted` 的推入对象也改为小写字段，避免后续调用出现同样问题。

3. 确认逻辑无需改动
- 继续使用 `onOk` 中的错误统计提示与部分忽略提示，成功后 `setItems(prev => [...parsedItems, ...prev])` 保持不变。

## 验证
- 粘贴含 5 列（不含零售价）且默认选择的用例：应显示 `货号/名称/条码/数量/进货价`，`零售价`为空。
- 勾选“零售价”后粘贴 6 列：应显示全部 6 列并计算 `金额=数量*进货价`。
- 未选择关键列（数量/进货价）时：阻断并提示，不添加空白行。

## 代码改动范围
- 仅修改 `ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceDetail/index.tsx` 中两个解析函数的字段映射；不改动表格列定义与弹窗 UI。

请确认后我将进行上述修改并回归测试。