## 修改点
- 隐藏供应商货号检测分列中的“商品编码”列，仅保留“检测结果”“商品名称”“当前进货价”“当前零售价”。
- 为“批内重复数”列增加前端排序：按本批次中条码或货号的重复次数排序（升/降）。

## 实施文件
- ReactUmi/my-app/src/pages/PosAdmin/LocalSupplierInvoiceCreate/index.tsx

## 具体实现
- 删除/移除列定义：删除 `{ title: '商品编码', key: 'supplierItemProductCode', ... }`。
- 在“批内重复数”列配置 `sorter`：`(a, b) => dupCount(a) - dupCount(b)`，其中 `dupCount(r)` 计算规则为当前导入数据中条码或货号的重复次数（OR）。

## 验证
- 表格中不再显示商品编码列；
- 点击“批内重复数”列表头可进行升降序排序，统计与颜色显示仍一致。

## 影响范围
- 仅前端创建页列定义，后端无改动。