## 修改计划

### 1. DateRangeFilterBar.tsx
- 将日期选择器、快速选择按钮、同比模式 Radio 和刷新按钮合并到同一行
- 使用 `Row` 和 `Col` 响应式布局
- 将周期显示的 Tag 整合到刷新按钮旁边

### 2. DashboardSummary.tsx  
- 将统计卡片的 `md={6}` 改为 `md={4}`，使6个卡片在大屏幕上一行显示

### 3. SalesDashboard/index.tsx
- 将 `DateRangeFilterBar` 移出 `Spin` 组件
- 将 `DateRangeFilterBar` 放在 `ProCard` 外面
- 为 `DateRangeFilterBar` 添加 `sticky` 定位，固定在页面顶部不随内容滚动