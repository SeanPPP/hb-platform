# 安装缺失依赖 'xlsx'

项目中缺少 `xlsx` 包，这是 Invoice 页面 Excel 导出功能所必需的。根据项目根目录存在 `pnpm-lock.yaml` 文件，我将使用 `pnpm` 进行安装。

## 1. 安装 xlsx
- 执行命令: `pnpm add xlsx`
- 目录: `d:\Development\cline\blazor\reactumi\my-app`

## 2. 验证
- 安装完成后，该依赖错误应会消失。用户可以重新启动开发服务器进行确认。
