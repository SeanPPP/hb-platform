## 实施计划

### 后端修改
1. **StoreDtos.cs** - 在以下 DTO 类中添加 `BrandName` 属性：
   - `CreateStoreDto` - `[StringLength(100)] public string? BrandName { get; set; }`
   - `UpdateStoreDto` - `[StringLength(100)] public string? BrandName { get; set; }`
   - `StoreDto` - `[StringLength(100)] public string? BrandName { get; set; }`
   - `StoreDetailDto` - `[StringLength(100)] public string? BrandName { get; set; }`

### 前端修改
1. **types/store.ts** - 在 TypeScript 接口中添加 `brandName?: string`
2. **StoreManagement/index.tsx** - 在表格列中添加品牌名称列（在分店代码后）
3. **CreateStoreForm.tsx** - 添加品牌名称表单项（可选字段，最大长度 100）
4. **UpdateStoreForm.tsx** - 添加品牌名称表单项和 useEffect 初始化
5. **StoreDetailDrawer.tsx** - 在详情描述中显示品牌名称

### 字段规格
- 类型：`string?`（可空）
- 最大长度：100 字符
- 非必填项