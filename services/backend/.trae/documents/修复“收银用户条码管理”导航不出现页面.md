## 实施计划

### 1. 在 `src/locales/zh-CN.ts` 添加中文翻译

添加约 100 个新的翻译 key，涵盖：

* 页面标题和按钮文本

* 表单标签和占位符

* 表格列标题

* 操作按钮和确认对话框

* 状态标签和筛选选项

* 消息提示（成功/失败/警告）

* 弹窗标题和内容

### 2. 在 `src/locales/en-US.ts` 添加英文翻译

为所有中文翻译添加对应的英文版本

### 3. 修改 `LocalSupplierInvoices/index.tsx`

* 引入 `useIntl` hook

* 替换所有硬编码的中文文本为 `intl.formatMessage({ id: '...' })`

* 涉及：页面标题、筛选项、按钮、表格列、操作、消息提示、弹窗等

### 4. 修改 `LocalSupplierInvoiceDetail/index.tsx`

* 引入 `useIntl` hook

* 替换所有硬编码的中文文本为 `intl.formatMessage({ id: '...' })`

* 涉及：页面标题、表单字段、表格列、操作按钮、消息提示、弹窗等

### 5. 测试验证

* 检查所有文本是否正确显示

* 切换语言验证翻译是否生效

* 确保动态参数（如 `{total}`）正确传递

## 预计修改的翻译 Key 数量

* 中文翻译：约 100 个 key

* 英文翻译：约 100 个 key

* 修改文件：4 个文件（2 个语言文件 + 2 个页面组件）

