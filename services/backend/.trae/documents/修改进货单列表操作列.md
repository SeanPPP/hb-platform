## 修改计划

修改 `LocalSupplierInvoices/index.tsx` 的操作列，添加两个按钮：

### 变更内容
1. **查看按钮** - 所有用户可见
   - 点击打开 `StoreInvoiceView` 组件
   - path: `/pos-admin/store-invoice-view/${record.invoiceGUID}`
   - component: `StoreInvoiceView`
   - key: `pos-admin-store-invoice-view-${record.invoiceGUID}`

2. **编辑按钮** - 仅管理员可见
   - 点击打开 `LocalSupplierInvoiceDetail` 组件（原有功能）
   - 保留现有的 path 和 component 配置

3. **删除按钮** - 仅管理员可见
   - 保留现有功能不变

### 操作列宽度
- 调整为 250 以容纳三个按钮