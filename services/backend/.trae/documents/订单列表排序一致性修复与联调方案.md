## 目标
- 订单列表前端“日期”列仅显示日期，不显示时间部分；与现有打印页一致使用 dd/MM/yyyy。

## 修改点
- 页面 [StoreOrderList.razor](file:///d:/Development/cline/blazor/BlazorApp/Pages/WareHousePages/StoreOrderList.razor)：
  - 列表渲染处将 `item.OrderDate` 的显示改为 `item.OrderDate?.ToString("dd/MM/yyyy")`。
  - 如存在自定义表格组件列模板，统一用上述格式化方法；若为字符串类型，先 `DateTime.Parse(item.OrderDate)` 再格式化。
- 统一格式：与打印页目前使用的 `orderDetail.LastModified.ToString("dd/MM/yyyy")` 保持一致，避免不同页面日期格式不统一。[StoreOrderList.razor:1218](file:///d:/Development/cline/blazor/BlazorApp/Pages/WareHousePages/StoreOrderList.razor#L1218)

## 可选增强
- 若存在多个页面使用订单日期显示，提取一个小型扩展方法或 helper（例如 `FormatDate(DateTime?)`）以统一调用。
- 对于跨时区场景，明确使用服务器时间或本地时间；默认按服务器返回的 DateTime 显示，不做时区转换。

## 验证
- 打开订单列表，确认日期列仅显示 dd/MM/yyyy。
- 对比打印页面格式，确保一致。
- 切换筛选与分页后，确保格式保持不变。

确认后我将直接在列表模板中更新日期显示格式为 dd/MM/yyyy。