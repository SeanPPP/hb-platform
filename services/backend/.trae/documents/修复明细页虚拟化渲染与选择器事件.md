## 细化要求
- 在明细页，当行数（items.length）大于 250 时启用稳定虚拟化方案A（react-data-grid）；否则使用现有 AntD 表格。

## 实施细节
- 新增 `DetailDataGrid`（仅明细页使用）：
  - `rowKeyGetter={(r) => r.detailGUID}`、`selectedRows` 与 `onSelectedRowsChange` 同步到 `selectedRowKeys`。
  - 列映射：保留现有列的标题与宽度，使用 `formatter` 实现图片、Tag、数值格式化；行高固定 48。
  - 只读展示为主，工具栏操作直接作用于 `items/rowMeta` 与现有 API；不改变工具栏。
- 明细页接入：
  - 判断 `items.length > 250` 渲染 `DetailDataGrid`，否则用 AntD 表格（现有 `VirtualTable`）。
- 验证：
  - 1万行数据滚动与选择流畅；执行工具栏功能（检测、计算、批量执行、保存、分发、粘贴导入）验证无回归。
- 回退：
  - 页面提供快速开关关闭虚拟化，必要时回到 AntD 表格。

若确认，我将按以上阈值 250 实施并验证。