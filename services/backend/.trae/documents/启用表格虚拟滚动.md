## 启用表格虚拟化

### 修改内容
在 `PosmSalesOrders/index.tsx` 的 ProTable 组件中添加 `scroll` 属性：

```tsx
scroll={{ x: 'max-content', y: 'calc(100vh - 400px)' }}
```

### 效果
- 固定表头，滚动时表头保持可见
- 启用垂直虚拟滚动，提升大数据量性能
- 支持水平滚动，适应列数较多的情况