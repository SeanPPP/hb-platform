1. 修改 `DateRangeFilterBar.tsx`：

   * 在 `handleQuickSelect` 函数中添加 `'today'` 和 `'yesterday'` 两个 case

   * 在按钮区域添加"今天"和"昨天"按钮

2. 修改 `locales/zh-CN.ts`：

   * 添加 `'salesDashboard.quickSelect.today': '今天'`

   * 添加 `'salesDashboard.quickSelect.yesterday': '昨天'`

3. 修改 `locales/en-US.ts`：

   * 添加 `'salesDashboard.quickSelect.today': 'Today'`

   * 添加 `'salesDashboard.quickSelect.yesterday': 'Yesterday'`

