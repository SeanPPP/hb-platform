修改 PosmSalesOrders 页面的折扣后金额列，将显示逻辑从直接使用 `actualAmount` 字段改为计算 `totalAmount - discountAmount`。

修改位置：`ReactUmi/my-app/src/pages/PosmSalesOrders/index.tsx` 第108-110行

具体修改：

* 原代码：`{(record.actualAmount || 0).toFixed(2)}`

* 修改为：`{((record.totalAmount || 0) - (record.discountAmount || 0)).toFixed(2)}`

