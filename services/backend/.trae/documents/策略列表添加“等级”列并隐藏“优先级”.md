## 目标
- 在“自动价格策略管理”列表中添加“等级（Level）”列，显示为中文（供应商/分店/全局）。
- 隐藏现有的“优先级”列。

## 实施
- 文件：`reactumi/my-app/src/pages/PosAdmin/PricingStrategies/index.tsx`
- 变更：
  - 在 `columns` 中移除 `priority` 列。
  - 新增 `level` 列，使用映射显示中文：`Supplier→供应商`、`Store→分店`、`Global→全局`。
- 说明：后端与前端类型已包含 `level` 字段（DTO与TS类型已同步），无需额外接口改动。

## 验证
- 列表页加载后显示“等级”列，且不再显示“优先级”。
- 其他列与排序、分页保持正常。