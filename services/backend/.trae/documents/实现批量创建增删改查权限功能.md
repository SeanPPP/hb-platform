收到，用户补充了需求：权限名称也需要对应加上后缀（查看/创建/删除/编辑）。

修正后的计划：

1.  **修改后端 DTO (`BlazorApp.Shared/DTOs/RoleDtos.cs`)**:
    *   在 `CreateSysPermissionDto` 中添加 `List<string> Actions` 属性。

2.  **修改后端服务 (`BlazorApp.Api/Services/RoleService.cs`)**:
    *   更新 `CreatePermissionAsync` 方法。
    *   如果 `Actions` 列表包含值，则遍历生成多个权限：
        *   **Code**: `{dto.Code}.{action}` (例如 `Orders.Create`)
        *   **Name**: `{dto.Name}{ActionSuffix}` (例如 `订单管理创建` 或 `订单管理 - 创建`)。为了更友好，我将建立一个 Action 到中文后缀的映射：
            *   `Create` -> `创建`
            *   `Delete` -> `删除`
            *   `Edit` -> `编辑`
            *   `View` -> `查看`
        *   **Description**: `{dto.Description} - {ActionSuffix}`
    *   如果 `Actions` 为空，则保持原有逻辑（创建单个权限）。
    *   返回值类型可能需要调整为 `List<SysPermission>` 或者仅返回成功状态，因为可能创建了多个。为了兼容现有前端，可以返回创建的第一个，或者修改返回类型。鉴于前端目前只判断 success，我将保持返回 `SysPermission` (可能是列表中的第一个) 或者修改 API 返回类型为 `List<SysPermission>`。为了最小化改动，我将修改 API 返回 `List<SysPermission>`，并更新前端服务接收类型。

3.  **修改前端 DTO (`src/types/role.ts`)**:
    *   更新 `CreateSysPermissionDto` 接口，增加 `actions?: string[]`。

4.  **修改前端国际化 (`src/locales/zh-CN.ts`, `src/locales/en-US.ts`)**:
    *   添加 `permission.form.actions.label`: '自动生成' / 'Auto Generate'
    *   添加 `permission.action.create`: '创建' / 'Create'
    *   添加 `permission.action.delete`: '删除' / 'Delete'
    *   添加 `permission.action.edit`: '编辑' / 'Edit'
    *   添加 `permission.action.view`: '查看' / 'View'

5.  **修改前端弹窗组件 (`CreatePermissionModal.tsx`)**:
    *   引入 `Checkbox` 组件。
    *   添加一个表单项 `actions`，使用 `Checkbox.Group`。
    *   选项值: `['View', 'Create', 'Edit', 'Delete']`。
    *   选项显示标签: 使用国际化 `permission.action.xxx`。
    *   默认选中所有（根据“即可生成4个权限”的暗示，或者默认全选方便用户）。
    *   当勾选了 actions 时，`code` 输入框的 label 提示变为 "基础代码 (Base Code)"，`name` 变为 "基础名称 (Base Name)"。

6.  **修改前端服务 (`src/services/roleService.ts`)**:
    *   更新 `createPermission` 的返回类型定义（如果后端改变了返回结构）。建议后端改为返回 `ApiResponse<List<SysPermission>>` 或 `ApiResponse<object>`，前端相应调整。

**执行顺序**:
1. 后端 DTO 和 Service 修改 (逻辑核心)。
2. 后端 Controller 修改。
3. 前端 Type 和 Locale 修改。
4. 前端组件修改。

**关于后端 Service 返回值的兼容性**:
原接口返回 `ApiResponse<SysPermission>`。
如果改为批量创建，最好返回 `ApiResponse<List<SysPermission>>`。
前端 `createPermission` 调用处目前是 `request<ApiResponse<SysPermission>>`，需要改为 `request<ApiResponse<any>>` 或对应的新类型，并在 Modal 中处理。

我将修改后端接口返回 `List<SysPermission>`。

