## 将要执行的改动
- 后端新增 DTO 与接口：批量更新条码、批量创建 ProductSetCode 行。
- 控制器新增端点：`PUT /product-set-codes/batch-barcodes`、`POST /product-set-codes/batch-create`。
- 服务实现更新与创建逻辑：不做条码唯一性校验，支持自动生成 `SetItemNumber`。
- 前端服务新增调用方法；编辑弹窗子表支持条码可编辑、添加新行并保存创建与更新。

## 预期效果
- 在“套装/多码”表中可新增条码行并录入条码与价格；保存后成功写库并刷新显示。
- 旧行条码与价格均可编辑并保存。