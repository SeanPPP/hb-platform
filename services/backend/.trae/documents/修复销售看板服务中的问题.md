## 修复计划

### 1. 修复 `GetHourlySalesAsync` 方法中对比查询缺少 GroupBy 的问题
- 在第259-267行的 `compareQuery` 中添加 `GroupBy(s => 1)` 以正确使用聚合函数

### 2. 优化 `GetHourlySalesAsync` 方法的性能
- 将 N+1 查询改为批量查询，先一次性获取所有对比数据，再进行内存匹配

### 3. 移除或实现 `GetHourlySalesAsync` 方法中未使用的 `supplierCode` 参数
- 需要与用户确认是移除该参数还是实现按供应商筛选功能

### 4. 修复 `GetSupplierStoreSalesAsync` 方法中分店名称获取错误
- 在查询时包含分店名称字段，或加载 Store 表映射以获取正确的分店名称

### 修改文件
- `BlazorApp.Api/Services/React/SalesDashboardReactService.cs`

### 待确认事项
- `GetHourlySalesAsync` 方法中的 `supplierCode` 参数是否需要实现按供应商筛选功能？