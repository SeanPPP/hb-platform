## 添加国际化支持计划

### 1. 在 `zh-CN.ts` 中添加中文翻译键值（约40+个键值）
- 页面标题和标签
- 表格列标题（15个）
- 表单字段标签和占位符
- 按钮文本
- 批量修改选项
- 提示消息（成功/失败/警告）
- 分页信息

### 2. 在 `en-US.ts` 中添加对应的英文翻译
- 所有中文键值的英文翻译

### 3. 修改 `StoreRetailPrices/index.tsx` 文件
- 引入 `useIntl` hook
- 将所有硬编码的中文文本替换为 `intl.formatMessage()` 调用
- 替换内容包括：
  - Card 标题
  - 所有表格列的 title
  - Form.Item 的 label 和 placeholder
  - Button 文本
  - Select 的 placeholder 和 options
  - Tag 文本
  - Popconfirm 的 title、okText、cancelText
  - 所有 message 提示
  - 分页的 showTotal
  - Checkbox 文本

预计修改数量：约60处文本替换