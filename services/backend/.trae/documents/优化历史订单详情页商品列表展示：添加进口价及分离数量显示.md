我将修改“历史订单详情页”的商品列表展示，以满足您的新需求。

### 1. 修改 StoreHistoryOrders/Detail.tsx

*   **当前结构**：使用 `List` 组件，`itemLayout="horizontal"`。
*   **需求分析**：
    *   **进口价格**：需要显示 `Import Price`。
    *   **数量分列**：订货数量 (Order Qty) 和发货数量 (Send Qty) 需要更清晰地分开展示，最好是单独的列或区域。

*   **修改方案**：
    *   调整 `List.Item` 的渲染结构。
    *   在 `description` 区域或 `content` 区域重新布局。
    *   **布局设计**：
        *   **行 1**：商品编号 (Item No)。
        *   **行 2 (Grid)**：
            *   列 1: Import Price (显示为 `$X.XX`)
            *   列 2: Order Qty
            *   列 3: Send Qty
            *   列 4: Total Amount (可选，或者放右侧)

    或者保持 `List` 但优化 `description` 的 Flex 布局：
    ```tsx
    <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr 1fr', gap: 8, marginTop: 8 }}>
       <div>
          <Text type="secondary" style={{ fontSize: 12 }}>Price</Text>
          <div>${item.price.toFixed(2)}</div> {/* 确认这是 Import Price 还是 OEM Price? DTO 中 price 可能是 OEM。需确认字段 */}
       </div>
       <div>
          <Text type="secondary" style={{ fontSize: 12 }}>Order</Text>
          <div>{item.quantity}</div>
       </div>
       <div>
          <Text type="secondary" style={{ fontSize: 12 }}>Sent</Text>
          <div>{item.allocQuantity || '-'}</div>
       </div>
    </div>
    ```

*   **字段确认**：
    *   查看 `StoreOrderCartItemDto` 定义。
    *   `price`: 通常是 OEM Price。
    *   `importPrice`: 进口价。
    *   用户要求显示“进口价格”。我需要确认前端 DTO 是否包含 `importPrice`。之前的 `Read` 结果显示 `StoreOrderCartItemDto` 包含 `importPrice`。

### 2. 执行计划
修改 `d:\Development\cline\blazor\ReactUmi\my-app\src\pages\StoreHistoryOrders\Detail.tsx`：

1.  在 `renderItem` 中，重构展示逻辑。
2.  添加“进口价格”显示：`Import: ${item.importPrice?.toFixed(2)}`。
3.  将“订货数量”和“发货数量”分为独立的列/块显示，使其更易读。

