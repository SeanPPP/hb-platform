1. 重构 `HourlySalesChart.tsx`，将折线图改为表格显示

2. **表格列定义**：

   * 小时（行头） - 显示 "9:00", "10:00" 等

   * 销售额 - 使用 `formatAmount` 格式化

   * 数量

   * 客户数

   * 平均客单价

3. **数据处理逻辑**：

   * 无分店过滤时：按小时聚合，显示所有分店的合计

   * 有分店过滤时：显示该分店的小时数据

4. **样式优化**：

   * 表格紧凑模式（small size）

   * 金额列右对齐

   * 数字列格式化

