为了实现权限管理的页面导航国际化以及添加创建新权限的前端入口，我将执行以下步骤：

1. **国际化配置**:

   * 在 `src/locales/zh-CN.ts` 和 `src/locales/en-US.ts` 中添加权限管理相关的国际化键值对。

   * 添加菜单项国际化：`menu.system.permission`: '权限管理' / 'Permission Management'。

   * 添加页面字段国际化：

     * `permission.title`: '权限管理' / 'Permission Management'

     * `permission.list`: '权限列表' / 'Permission List'

     * `permission.add`: '新建权限' / 'New Permission'

     * `permission.code`: '权限代码' / 'Permission Code'

     * `permission.name`: '权限名称' / 'Permission Name'

     * `permission.category`: '分类' / 'Category'

     * `permission.description`: '描述' / 'Description'

     * `permission.createdAt`: '创建时间' / 'Created At'

     * `permission.assignRole`: '分配角色' / 'Assign Role'

     * `permission.form.code.placeholder`: '请输入权限代码 (例如: User.View)' / 'Please enter permission code (e.g., User.View)'

     * `permission.form.name.placeholder`: '请输入权限名称' / 'Please enter permission name'

     * `permission.form.category.placeholder`: '请输入或选择分类' / 'Please enter or select category'

     * `permission.form.description.placeholder`: '请输入描述' / 'Please enter description'

     * `permission.message.createSuccess`: '权限创建成功' / 'Permission created successfully'

     * `permission.message.createFailed`: '权限创建失败' / 'Failed to create permission'

2. **创建权限组件**:

   * 在 `src/pages/PermissionManagement/components` 目录下创建 `CreatePermissionModal.tsx` 组件。

   * 该组件将包含一个表单，用于输入权限代码、名称、分类和描述。

   * 使用 `ProForm` 或 `ModalForm` 来构建表单。

   * 调用后端的 `createPermission` API (需要确认 `roleService` 中是否已导出该方法，如果没有则需要在前端 service 层添加)。

3. **更新服务层**:

   * 检查 `src/services/roleService.ts`，确保包含 `createPermission` 方法，用于调用后端 `POST /api/roles/permissions` 接口。

4. **更新权限管理页面**:

   * 修改 `src/pages/PermissionManagement/index.tsx`。

   * 引入 `useIntl` 钩子用于国际化。

   * 替换现有的硬编码文本为 `intl.formatMessage` 调用。

   * 在表格工具栏添加“新建权限”按钮。

   * 引入并使用 `CreatePermissionModal` 组件。

5. **更新路由/菜单配置**:

   * 检查 `config/routes.ts` 或 `src/app.tsx` (或其他定义菜单的地方)，确保“权限管理”菜单项使用了正确的国际化 key (`menu.system.permission`)。根据之前的 LS 结果，菜单似乎是通过后端返回或在 `locales` 文件中定义的 key 匹配的。我会确保 `locales` 文件中有对应的 key。

**注意**: 后端接口 `POST api/roles/permissions` 已经在之前的步骤中实现。

**执行步骤**:

1. 更新 `src/locales/zh-CN.ts` 和 `src/locales/en-US.ts`。
2. 检查并更新 `src/services/roleService.ts`。
3. 创建 `src/pages/PermissionManagement/components/CreatePermissionModal.tsx`。
4. 更新 `src/pages/PermissionManagement/index.tsx` 以集成国际化和创建功能。

