## 修复方案

### 修改前端请求参数构建

在 [index.tsx](d:\Development\cline\blazor\ReactUmi\my-app\src\pages\SystemSettings\DeviceManagement\index.tsx) 的 `request` 函数中，添加设备类型和设备系统的过滤处理：

```typescript
const filterModel: any = {};

if (params.分店代码) {
  filterModel['分店代码'] = {
    filter: params.分店代码,
    filterType: 'text',
    type: 'equals',
  };
}

if (params.设备类型) {
  filterModel['设备类型'] = {
    filter: params.设备类型,
    filterType: 'text',
    type: 'equals',
  };
}

if (params.设备系统) {
  filterModel['设备系统'] = {
    filter: params.设备系统,
    filterType: 'text',
    type: 'equals',
  };
}

if (params.设备状态) {
  filterModel['设备状态'] = {
    filter: params.设备状态,
    filterType: 'text',
    type: 'equals',
  };
}
```

### 执行步骤
修改 `index.tsx` 文件中的 `request` 函数，在 `filterModel` 构建部分添加设备类型和设备系统的过滤处理。