好的，我将开始执行代码修改，包括种子数据的更新。

**执行步骤如下：**

1.  **前端 (`ReactUmi/my-app`)**
    *   安装 `crypto-js` 依赖。
    *   **登录页 (`Login/index.tsx`)**：在发送请求前，对密码进行 SHA256 哈希。
    *   **用户管理 (`UserManagement/index.tsx`)**：在创建用户时，对初始密码进行 SHA256 哈希。

2.  **后端 (`BlazorApp.Api`)**
    *   **工具类 (`Utils/PasswordHasher.cs`)**：
        *   添加 `ComputeSha256` 静态方法。
        *   更新 `CheckPasswordStrength` 方法以兼容哈希值。
    *   **用户服务 (`Services/UserService.cs`)**：
        *   修改 `ResetUserPasswordAsync`（重置密码）逻辑，先计算 SHA256 再存储。
        *   修改 `ImportUsersAsync`（导入用户）逻辑，先计算 SHA256 再创建。
    *   **种子数据 (`Services/SeedDataService.cs`)**：
        *   修改 `CreateAdminUserAsync`，将默认密码 "admin" 先通过 `ComputeSha256` 哈希，再传给 `HashPassword` 存储。这确保了重置数据库后，默认管理员账号能通过前端（发送哈希值的）登录页成功登录。

**提示**：执行完毕后，您可能需要删除现有的 SQLite 数据库文件（`BlazorApp.db`）并重启后端，以便重新生成带有正确哈希密码的种子数据。
