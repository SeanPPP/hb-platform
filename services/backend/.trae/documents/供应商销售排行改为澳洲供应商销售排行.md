1. **添加国际化 key**：

   * `zh-CN.ts`: 添加 `'salesDashboard.ozSupplierRankTitle': '澳洲供应商销售排行'`

   * `en-US.ts`: 添加 `'salesDashboard.ozSupplierRankTitle': 'Australia Supplier Sales Rank'`

2. **修改 SupplierSalesRankTable** 默认标题：

   * 将 `salesDashboard.supplierRankTitle` 改为 `salesDashboard.ozSupplierRankTitle`

3. **关于"只显示本地供应商数据"**：

   * 需要确认后端 API 是否有参数区分本地/国际供应商

   * 或者数据中是否有字段标识供应商类型

   * 当前需要更多信息确认如何实现过滤

