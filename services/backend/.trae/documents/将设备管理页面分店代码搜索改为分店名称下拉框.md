修改 `DeviceManagement/index.tsx` 文件：

1. **添加导入**：

   * 导入 `useEffect` hook

   * 导入 `storeService.getAllStoresByName`

2. **添加状态**：

   * 添加 `storeOptions` 状态（类型为 `Record<string, { text: string }>`）

3. **获取分店数据**：

   * 使用 `useEffect` 在组件加载时调用 `getAllStoresByName()`

   * 将返回的分店列表转换为 valueEnum 格式

4. **修改列定义**：

   * 将第 112 行列标题从"分店代码"改为"分店名称"

   * 添加 `valueType: 'select'`

   * 配置 `valueEnum` 使用 `storeOptions`

5. **过滤逻辑保持不变**：

   * 继续使用 `params.分店代码` 作为查询参数（确保后端兼容）

