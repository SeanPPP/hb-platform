## 问题与原因
- 点击“仓库管理/分类管理”后页面空白。对比“货柜列表”能正常显示，初步定位为 Tab 布局未能加载对应组件：在 `KeepAliveTabLayout.tsx` 的 `componentMap` 中没有 `WarehouseCategories` 映射，导致渲染时返回“组件未找到”，从而显示空白。
- 同时你希望删除“组件测试”导航及相关页面代码，以精简菜单并避免干扰。

## 修复与清理方案
### 1. 修复分类页不显示
- 在 `ReactUmi/my-app/src/layouts/KeepAliveTabLayout.tsx` 的 `componentMap` 中新增：
  - `WarehouseCategories: React.lazy(() => import('@/pages/WarehouseCategories') as any)`
- 说明：与现有的 `ContainerList`、`ChinaSupplier` 等一致，让 Tab 布局能渲染该页面组件。

### 2. 删除“组件测试”导航与页面
- 路由：在 `.umirc.ts` 中移除 `componentTest` 组及内部 `enhancedDataGrid` 子路由。
- 页面代码：删除 `ReactUmi/my-app/src/pages/EnhancedDataGridTest/` 整个目录（不影响其他功能；此页仅为演示测试）。
- 布局映射：从 `KeepAliveTabLayout.tsx` 的 `componentMap` 中移除 `EnhancedDataGridTest` 映射。

### 3. 构建与验证
- 重新构建前端，确保菜单点击“仓库管理/分类管理”可以正常渲染，控制台无“组件未找到”或 React Intl 缺失提示。
- 验证路由联通：
  - 打开“货柜列表”确认不受影响。
  - 打开“分类管理”，左侧出现分类树，右侧为商品列表。

## 变更范围与安全
- 仅前端文件：`KeepAliveTabLayout.tsx`、`.umirc.ts`、`src/pages/EnhancedDataGridTest`（删除）。不改动后端。
- 不影响其他页面与服务；与现有权限控制一致（`access: 'isAdmin'`）。

请确认以上计划，确认后我将实施修改并完成构建与验证。