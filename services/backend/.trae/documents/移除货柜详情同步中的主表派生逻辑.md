## 目标
- 修改后端同步方法：`SyncContainerDetailsFromHqAsync` 不再派生或写入 `Container` 主表，仅同步 `ContainerDetail`。
- 保持事务与日志结构不变，避免对现有数据造成影响。
- 更新方法注释及前端同步页的文案以保持一致性。

## 具体改动
### 后端服务
- 文件：`BlazorApp.Api/Services/React/DataSyncReactService.cs`
- 方法：`SyncContainerDetailsFromHqAsync`
  1. 删除本地清理 `Container` 的代码（仅清理 `ContainerDetail`）。
  2. 删除派生 `distinctMasters` 与 `Container` 批量写入的整段逻辑。
  3. 更新 XML 注释：改为“不派生主表 Container，仅同步详情”。
  4. 保持后续分页读取与 `ContainerDetail` 的批量写入逻辑不变。

### 前端页面文案
- 文件：`ReactUmi/my-app/src/pages/SystemSettings/HqDataSync/index.tsx`
- 卡片标题与说明：
  - 将 “货柜详情（CPT_RED_货柜单详情表 → ContainerDetail + Container）” 修改为 “货柜详情（CPT_RED_货柜单详情表 → ContainerDetail）”。
  - 将副标题 “自动派生主表（Container）” 修改为仅说明“全量同步，批量写入”。

## 验证
- 后端：`dotnet build` 编译通过。
- 前端：`npm run build` 构建通过。
- 调用 `POST /api/react-sync/container-details` 验证不再写入 `Container`。

## 影响范围与回滚
- 仅影响详情同步方法，对独立的主表同步方法 `SyncContainersFromHqAsync` 无影响。
- 如需恢复派生逻辑，可回滚删除的代码块或改为配置开关控制。