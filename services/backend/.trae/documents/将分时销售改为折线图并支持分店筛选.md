1. 修改 `src/services/salesDashboard.ts`：

   * 更新 `getHourlySales` 函数签名，增加 `branchCodes` 参数。

2. 修改 `src/pages/SalesDashboard/index.tsx`：

   * 恢复引入 `HourlySalesChart` 组件。

   * 在 `loadDashboardData` 函数中，调用 `getHourlySales` 时传入 `selectedBranchCode`（如果已选择）。

   * 在 `useEffect` 的依赖数组中添加 `selectedBranchCode`，以便在选择分店时自动刷新数据。

   * 将 `<HourlySalesTable ... />` 替换回 `<HourlySalesChart ... />`。

