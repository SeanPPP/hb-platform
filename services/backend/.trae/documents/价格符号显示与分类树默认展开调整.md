分析与计划
- 价格符号：国内价格显示为“￥”，进口价格与贴牌价格显示为“$”。在商品列表列定义中为三列添加渲染函数以带符号展示，保留现有排序与过滤。
  - 修改位置：ReactUmi/my-app/src/pages/WarehouseCategories/index.tsx:76、77、78 三个列配置，分别为 domesticPrice、importPrice、oemPrice。
  - 修改方式示例：将列对象增加 render: (v) => v != null ? `￥${v}` : null（国内），以及 `$${v}`（进口、贴牌）。如需保留两位小数，可使用 `${v.toFixed(2)}`，请确认是否需要统一小数格式。

- 分类树默认展开：按照截图默认显示为折叠状态（有“+”号），不自动展开首层/次层。
  - 修改位置：ReactUmi/my-app/src/pages/WarehouseCategories/index.tsx:141-150（defaultExpandedKeys 计算）。
  - 修改方式：将 defaultExpandedKeys 设置为空数组 []，保持 Tree 的 defaultExpandedKeys={[]}（不展开）。无需设置 treeDefaultExpandAll。

执行步骤
1) 更新 index.tsx 中三处价格列 render，分别加上 ￥ 与 $。
2) 调整 defaultExpandedKeys 为 []，使分类树默认折叠显示，符合截图效果。
3) 本地运行页面，确认：
   - 国内价格列前缀为“￥”，进口与贴牌列为“$”。
   - 分类树加载后默认折叠（显示“+”号）。
4) 如需统一价格小数位或千分位格式，请确认具体规则后再调整。

请确认以上计划，我将按步骤实施并回传结果。