1. 修改 `formatChartData()` 函数逻辑

   * 从数据中提取实际存在的小时（有 totalAmount > 0 的小时）

   * 只为这些小时创建图表数据

   * 移除固定的 0-23 小时遍历

2. 具体改动：

   * 将 `Array.from({ length: 24 }, (_, i) => i)` 改为从数据动态提取小时

   * 过滤掉 totalAmount 为 0 或 null 的记录

