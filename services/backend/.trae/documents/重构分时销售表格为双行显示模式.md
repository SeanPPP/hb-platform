修改 `HourlySalesTable.tsx`：

1. **数据结构重构**：

   * 将每个分店的数据拆分为2行（本期、去年同期）

   * 添加类型标识字段区分两行

2. **动态列生成**：

   * 扫描所有数据，只收集有销售额的小时（totalAmount > 0 或 compareTotalAmount > 0）

   * 只显示这些时间列

3. **表格配置修改**：

   * 移除分页配置

   * 添加分组行样式（本期行和去年同期行用不同背景色区分）

4. **行结构**：

   * 基础信息列：分店代码、分店名称、总金额（只在本期行显示）

   * 时间列：本期行显示本期金额，去年同期行显示去年同期金额

