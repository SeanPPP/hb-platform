目标

- 分页显示在表格上方，靠右对齐。
- 保持单屏布局与内部滚动不变，表格滚动高度精确扣除顶部分页占位。

实现步骤

1) 修改 Table 分页位置
- 设置 `pagination.position = ['topRight']`，取消底部分页。

2) 保持高度计算
- 已有 `paginationHeight` 参与 `tableScrollY` 计算（index.tsx:193-204），继续扣除顶部分页高度，确保分页可见且表格在其下滚动。

3) 样式与滚动
- `warehouse-table-wrapper` 维持 `overflow: hidden`，垂直滚动由 `Table` 的 `scroll.y` 控制；工具栏与表头保持固定。