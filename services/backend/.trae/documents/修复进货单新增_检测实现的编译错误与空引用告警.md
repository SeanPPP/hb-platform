## 具体修复
- GUID 生成统一替换为 `UuidHelper.GenerateUuid7()`：
  - `BlazorApp.Api/Services/React/LocalSupplierInvoicesReactService.cs` 中创建主表与明细的两处 GUID 生成全部改为 `UuidHelper.GenerateUuid7()`。
- 引入必要命名空间：
  - 在上述服务文件顶部添加 `using BlazorApp.Shared.Helper;` 与 `using System.Linq;`。
- 修正条码匹配计数与可空性：
  - `codes = ...Distinct().ToList()` 后使用 `var count = codes.Count;`；`OverTwo = count > 2`。
  - `names` 列表在过滤非空后追加 `.Select(n => n!)`，并在多码映射处用 `Name = m.Product != null ? m.Product.ProductName : null`。

## 验证
- 编译通过，错误消失：
  - 不存在名称“Database”的错误消除。
  - “>” 运算符错误消除（对 `Count` 使用属性/方法正确）。
  - `List<string?>` → `List<string>` 可空性警告消除。
- 功能保持不变：创建进货单与两类检测接口行为不变。

## 影响范围
- 仅限服务实现的技术性修复，不涉及业务改动。