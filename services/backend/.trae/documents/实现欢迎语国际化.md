我将实现欢迎语的国际化功能，使其能够根据语言设置显示不同的文本。

### 计划
1.  **添加国际化键**: 在 `src/locales/zh-CN.ts` 和 `src/locales/en-US.ts` 中添加欢迎语的翻译键。
2.  **修改 Guide 组件**: 更新 `src/components/Guide/Guide.tsx` 使用国际化 API (`formatMessage`) 来显示欢迎语。

### 实施步骤
1.  **修改 `src/locales/zh-CN.ts`**:
    *   添加 `'guide.welcome': '欢迎使用 Hot Bargain ！'`
2.  **修改 `src/locales/en-US.ts`**:
    *   添加 `'guide.welcome': 'Welcome to Hot Bargain !'`
3.  **修改 `src/components/Guide/Guide.tsx`**:
    *   引入 `useIntl` 或 `formatMessage`
    *   使用国际化键替换硬编码的 "欢迎使用 Hot Bargain ！"